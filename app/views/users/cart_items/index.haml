%h2 カート追加済み商品一覧
%p ※カート内のアイテムは同一業者で揃えてください
- @cart_items.each do |cart_item|
  %p
    = cart_item.product.name
    #{cart_item.amount}個
    #{cart_item.product.seller.name}
    計#{cart_item.product.price * cart_item.amount}円
    = link_to '詳細', users_cart_item_path(cart_item)
    = link_to '削除', users_cart_item_path(cart_item), method: :delete
= link_to 'カート内のアイテムを購入する', new_users_order_path if @cart_items.present?
= paginate @cart_items
