%h2 カート追加済み商品一覧
- @cart_items.each do |cart_item|
  %p
    = cart_item.product.name
    #{cart_item.amount}個
    = link_to '詳細', users_cart_item_path(cart_item)
    = link_to '削除', users_cart_item_path(cart_item), method: :delete
= paginate @cart_items
