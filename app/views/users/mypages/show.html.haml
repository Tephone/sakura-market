%h2 #{@user.name}さん
%p= image_tag @user.image.url if @user.image.present?
%p= @user.email
%p= @user.nickname
%p= @user.address

= link_to '編集', edit_user_registration_path
= link_to '取得済みクーポン', users_my_coupon_path
