%h2 購入履歴
- @orders.each do |order|
  - order.order_products.each do |order_product|
    %p
      = l order_product.order.delivery_date
      = order_product.product.name
      #{order_product.amount}個
      = change_JPY(order_product.product.price * order_product.amount)
      = link_to '商品詳細', users_product_path(order_product.product)
= paginate @orders