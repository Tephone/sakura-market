%h2 商品一覧
- @products.each do |product|
  %p
    = product.name
    = link_to '詳細', admins_product_path(product)
    = link_to '削除', admins_product_path(product), method: :delete
= link_to '商品を登録する', new_admins_product_path

= paginate @products
